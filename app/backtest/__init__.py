"""Backtest module — historical strategy simulation and analysis.

Sub-packages:
    engine/     — Core simulation logic (BacktestEngine, PortfolioEngine, metrics, curves)
    exchange/   — Mock exchange simulation (MockExchange, order executor)
    reporting/  — Output generation (HTML reports, CSV/JSON export, batch reports)
    data/       — Data acquisition and management (OHLCV download, tick download)
    runners/    — Execution modes (batch, portfolio, tick-replay)
    optimization/ — Parameter optimization (placeholder)
    statistics/   — Statistical analysis (placeholder)
"""
