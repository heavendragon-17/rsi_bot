# Deploy configuration (VPS-only, not committed)
# These are not needed for the polling-based deploy — the deploy
# system uses git fetch + systemd timer. This file documents the
# paths used by deploy scripts for reference.

BOT_DIR=/opt/rsi_bot
STATUS_FILE=/tmp/rsi_bot_status.json
VERSION_FILE=/opt/rsi_bot/VERSION
