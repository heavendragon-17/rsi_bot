# Deploy configuration — reference only.
# All deploy paths are centralized in deploy/deploy_env.sh.
# Edit that file to change BOT_DIR or any other path, then run:
#
#   sudo deploy/install.sh
#
# to regenerate systemd services with the new paths.
