# TEMPLATE — Do not install directly.
# Run: sudo deploy/install.sh
# This generates the real service file from deploy/deploy_env.sh.
#
# Shown here for reference only.

[Unit]
Description=Check and deploy RSI Bot if new version available
After=network-online.target
Wants=network-online.target

[Service]
Type=oneshot
User=__SERVICE_USER__
WorkingDirectory=__BOT_DIR__
ExecStart=__BOT_DIR__/deploy/check_deploy.sh
StandardOutput=journal
StandardError=journal
