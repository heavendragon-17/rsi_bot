# Deprecated Code

This directory holds files that have been superseded by the codebase restructure.
They are kept for reference but are **not imported** by any active module.

If you need functionality from a file here, find its replacement in the main
`app/` tree (see `SPEC_CLEANUP_2_MIGRATION.md` for the migration table).
