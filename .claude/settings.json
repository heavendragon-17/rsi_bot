{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | grep -qE '^git commit' && bash scripts/arch_lint_hook.sh || true",
            "timeout": 30,
            "statusMessage": "Running architecture lint..."
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash scripts/arch_lint_post_edit.sh",
            "timeout": 30,
            "statusMessage": "Checking architecture rules..."
          }
        ]
      }
    ]
  }
}
